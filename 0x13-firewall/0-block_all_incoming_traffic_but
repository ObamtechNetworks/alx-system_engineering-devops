sudo apt update -y
sudo apt install ufw -y
sudo ufw default deny incoming  # set fire rules
sudo ufw default allow outgoing
sudo ufw allow 22
sudo ufw allow 443
sudo ufw allow 80
sudo ufw enable  # enable UFW to activate the set rules
sudo ufw status verbose  # check status of of ufw
